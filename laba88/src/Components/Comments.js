import React, { useState } from "react";
import { getComments as getCommentApi } from "../api";

export default function Components({ CurrentUserId }) {
     const [backendComments, setBackendComments] = useState([]);
     return;
}
